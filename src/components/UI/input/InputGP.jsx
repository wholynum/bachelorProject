import React from 'react';
import styles from './InputGP.module.css'

const InputGP = (props) => {
    return (
        <input className={styles.inputgp} {...props}/>
    );
};

export default InputGP;